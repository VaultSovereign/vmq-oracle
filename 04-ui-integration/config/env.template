# VaultMesh Q Business - UI Integration Environment Configuration
# Copy this to .env.local in your Next.js project root

# AWS Configuration (Server-side only - NEVER expose to browser)
AWS_REGION=eu-west-1
AWS_S3_BUCKET=vaultmesh-knowledge-base
AWS_ACCOUNT_ID=509399262563

# Lambda Invocation Mode
# - direct: Call Lambda functions directly via AWS SDK (requires IAM role)
# - apigw: Call via API Gateway (if you add one later)
INVOKER_MODE=direct

# Optional: OPA Policy Server
# If set, server will pre-check authorization before invoking Lambda
# Leave empty to rely on Lambda's built-in policy gate
OPA_URL=

# Anonymous Mode Defaults (remove after SSO is enabled)
# These provide fallback values when user identity isn't available
DEFAULT_GROUP=VaultMesh-Engineering
DEFAULT_USER_ID=anon@vaultmesh.io

# Logging
LOG_LEVEL=info

# Feature Flags
# ENABLE_ACTION_METRICS=true
# ENABLE_OPA_PRECHECK=false
